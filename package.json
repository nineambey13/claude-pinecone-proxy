{
  "name": "api-proxy-server",
  "version": "1.0.0",
  "description": "Server-side proxy for Claude and Pinecone API calls",
  "main": "render-server.js",
  "engines": {
    "node": ">=16.x"
  },
  "scripts": {
    "start": "node render-server.js",
    "dev": "nodemon render-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
